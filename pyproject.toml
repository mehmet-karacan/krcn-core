[build-system]
requires = []
build-backend = "krcn_build_backend"
backend-path = ["build_backend"]

[project]
name = "krcn-core"
version = "0.1.0"
description = "Local-first context, knowledge, memory, and orchestration core"
requires-python = ">=3.11"
authors = [
  { name = "Mehmet KARACAN" }
]
dependencies = []

[project.scripts]
krcn = "krcn_core.cli.app:main"
krcn-context = "krcn_core.repository_context:main"
krcn-verify = "krcn_core.foundation:main"
