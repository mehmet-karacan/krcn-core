{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:krcn:schemas:application-response:1",
  "title": "KRCN Core transport-neutral application response",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "request_id", "operation", "status", "data"],
  "properties": {
    "schema_version": { "const": 1 },
    "request_id": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "operation": {
      "enum": [
        "installation.inspect",
        "installation.verify",
        "release.diff",
        "release.merge",
        "deployment.rollback",
        "project.list",
        "project.inspect",
        "project.onboard",
        "project.rescan",
        "knowledge.catalog",
        "knowledge.search-exact",
        "knowledge.search-dependencies",
        "knowledge.search-semantic",
        "context.build",
        "memory.propose",
        "memory.review",
        "memory.persist",
        "memory.lifecycle",
        "orchestrator.intent",
        "orchestrator.plan",
        "orchestrator.authorize",
        "orchestrator.start",
        "orchestrator.execute",
        "orchestrator.verify",
        "orchestrator.status",
        "orchestrator.resume"
      ]
    },
    "status": { "enum": ["ok", "planned", "applied"] },
    "data": { "type": "object", "additionalProperties": true }
  }
}
